# 1. This flag is CRITICAL. It tells pip to look for the small CPU versions.
--extra-index-url https://download.pytorch.org/whl/cpu

# 2. Relaxed version constraints to ensure compatibility with Streamlit's Linux environment
torch>=2.0.0
torchvision>=0.15.0
numpy
pandas
Pillow
streamlit